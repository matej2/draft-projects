
services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile-app
    ports:
      - "8000:8000"
  mongodb:
    build:
      context: .
      dockerfile: ./Dockerfile-mongo
    ports:
      - "27017:27017"